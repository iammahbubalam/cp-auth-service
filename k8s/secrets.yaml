Purpose: Stores sensitive data (like passwords, API keys, tokens) securely in Kubernetes.
What it does:
  Injects secrets into your pods as environment variables or files.
  Keeps secrets out of your codebase and Docker images.
